# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
    push:
        branches:
            - master
jobs:
    build_and_deploy:
        runs-on: ubuntu-latest
        env:
            PRODUCTION: "true"
            CIPHER_KEY: "${{ secrets.CIPHER_KEY }}"
            BACKEND_URL: "https://traineesqd-api.herokuapp.com"
        steps:
            - name: Cache Project npm dependencies
              uses: actions/cache@v2
              with:
                  path: "./node_modules"
                  key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
                  restore-keys: |
                      ${{ runner.os }}-node-

            - name: Install dependencies
              run: npm i

            - uses: actions/setup-node@v2
              with:
                  node-version: 16
            - run: node ci-script.js

            - uses: actions/checkout@v2
            - run: npm run build
              env:
                  PRODUCTION: "true"
                  CIPHER_KEY: "${{ secrets.CIPHER_KEY }}"
                  BACKEND_URL: "https://traineesqd-api.herokuapp.com"

            - uses: FirebaseExtended/action-hosting-deploy@v0
              with:
                  repoToken: "${{ secrets.GITHUB_TOKEN }}"
                  firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_TRAINEESQD }}"
                  channelId: live
                  projectId: traineesqd
